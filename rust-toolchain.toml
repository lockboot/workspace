# Default toolchain for the workspace.
#
# This is the fallback used when running rustup/cargo from the workspace root.
# Each sub-repo pins its own toolchain via its own rust-toolchain.toml (nearest
# file wins); stage0 and stage1 both pin 1.91.1 (kept in lockstep -- see
# REPRODUCIBLE-BUILDS.md). All resolved toolchains are installed once into the
# shared RUSTUP_HOME ($WORKSPACE/.rustup).
[toolchain]
channel = "1.91.1"
components = ["rustfmt", "clippy"]
targets = ["x86_64-unknown-linux-musl", "aarch64-unknown-linux-musl"]
profile = "minimal"
